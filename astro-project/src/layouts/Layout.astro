---
import CookieConsent from '../components/CookieConsent.astro';
import AccessibilityButton from '../components/AccessibilityButton.astro';

interface Props {
  title?: string;
}

const { title = "רועיטק - Automation Solutions" } = Astro.props;
---

<!DOCTYPE html>
<html lang="he" dir="rtl" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="AI chatbots, custom CRMs, and workflow automation for modern businesses" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <link rel="stylesheet" href="/src/styles.css" />
    <script is:inline>
      // Dark mode logic - runs immediately to prevent flash
      const theme = (() => {
        if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {
          return localStorage.getItem('theme');
        }
        if (window.matchMedia('(prefers-color-scheme: dark)').matches) {
          return 'dark';
        }
        return 'light';
      })();
      
      if (theme === 'light') {
        document.documentElement.classList.remove('dark');
      } else {
        document.documentElement.classList.add('dark');
      }
      
      window.localStorage.setItem('theme', theme);
    </script>
  </head>
  <body class="bg-stone-50 dark:bg-zinc-950 text-stone-900 dark:text-stone-100 transition-colors font-sans antialiased">
    <slot />
    <CookieConsent />
    <AccessibilityButton />
    
    <script is:inline>
      // Phone formatting for all forms
      document.querySelectorAll('input[type="tel"]').forEach(input => {
        input.addEventListener('input', function() {
          let value = this.value.replace(/\D/g, '');
          if (value.length > 0 && !value.startsWith('0')) {
            value = '0' + value;
          }
          if (value.length <= 3) {
            this.value = value;
          } else if (value.length <= 6) {
            this.value = value.slice(0, 3) + '-' + value.slice(3);
          } else {
            this.value = value.slice(0, 3) + '-' + value.slice(3, 6) + '-' + value.slice(6, 10);
          }
        });
      });
    </script>
    
    <script is:inline>
      // Theme toggle functionality
      function toggleTheme() {
        const html = document.documentElement;
        const current = html.classList.contains('dark') ? 'dark' : 'light';
        const next = current === 'dark' ? 'light' : 'dark';
        
        if (next === 'light') {
          html.classList.remove('dark');
        } else {
          html.classList.add('dark');
        }
        
        localStorage.setItem('theme', next);
      }
      
      // Make toggle function available globally for the button
      window.toggleTheme = toggleTheme;
    </script>
  </body>
</html>
